<Window x:Class="FileManager.winMain"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:FileManager"
        mc:Ignorable="d"
        Title="File Manager" Height="450" Width="469" BorderThickness="1" ResizeMode="CanResizeWithGrip" Icon="include/folderEmpty.ico" Background="#FF464646" Foreground="White" FontFamily="Arial" FontSize="14" WindowStyle="None" Initialized="Window_Initialized" Loaded="Window_Loaded" Closing="Window_Closing" >
  <Window.Resources>
    <!--<Style TargetType="{x:Type local:winMain}">
      <Setter Property="Background" Value="#FF464646"/>
      <Setter Property="Foreground" Value="White"/>
      <Setter Property="FontFamily" Value="Arial"/>
      <Setter Property="FontSize" Value="14"/>
      <Setter Property="VerticalContentAlignment" Value="Center"/>
    </Style>-->
    <Style TargetType="{x:Type Label}">
      <!--<Setter Property="Background" Value="Transparent"/>-->
      <Setter Property="Foreground" Value="White"/>
      <Setter Property="FontFamily" Value="Arial"/>
      <Setter Property="FontSize" Value="14"/>
      <Setter Property="VerticalContentAlignment" Value="Center"/>
    </Style>
    <Style TargetType="{x:Type TextBox}">
      <!--<Setter Property="Background" Value="#FF9C9C9C"/>-->
      <Setter Property="Foreground" Value="Black"/>
      <Setter Property="FontFamily" Value="Arial"/>
      <Setter Property="FontSize" Value="14"/>
      <Setter Property="VerticalContentAlignment" Value="Center"/>
    </Style>
    <Style TargetType="{x:Type Button}">
      <Setter Property="Background" Value="#FF9C9C9C"/>
      <Setter Property="Foreground" Value="White"/>
      <Setter Property="FontFamily" Value="Arial"/>
      <Setter Property="FontSize" Value="14"/>
      <Setter Property="VerticalContentAlignment" Value="Center"/>
    </Style>
  </Window.Resources>
  <DockPanel>
    <Grid DockPanel.Dock="Top">
      <Grid.ColumnDefinitions>
        <ColumnDefinition Width="Auto"/>
        <!-- App Icon/button -->
        <ColumnDefinition Width="Auto"/>
        <!-- App Title -->
        <ColumnDefinition/>
        <!-- App Message -->
        <ColumnDefinition Width="Auto"/>
        <!-- App Version -->
        <ColumnDefinition Width="Auto"/>
        <!-- App Minimize button -->
        <ColumnDefinition Width="Auto"/>
        <!-- App Maximize button -->
        <ColumnDefinition Width="Auto"/>
        <!-- App Close button -->
      </Grid.ColumnDefinitions>
      <!-- App Icon/button -->
      <Button x:Name="btnApp" Width="32" Height="32" BorderThickness="1" BorderBrush="White" VerticalAlignment="Top" Click="BtnApp_Click" Background="Transparent">
        <Image Source="include\folderEmpty.ico" Width="24" Height="24"/>
      </Button>
      <Border Grid.Column="1" BorderThickness="0,0,1,0" BorderBrush="White">
        <!-- App Title -->
        <TextBlock x:Name="txtAppName" Text="File Manager" Margin="5,0" VerticalAlignment="Center"/>
      </Border>
      <Border Grid.Column="2">
        <!-- App Title -->
        <TextBlock x:Name="txtStatus" Text="Message" Margin="5,0" VerticalAlignment="Center" TextWrapping="Wrap" TextTrimming="CharacterEllipsis"/>
      </Border>
      <!-- App Version -->
      <Border Grid.Column="3">
        <TextBlock x:Name="txtVersion" Text="1.0.0" Margin="5,0" VerticalAlignment="Center"/>
      </Border>
      <!-- App Minimize button -->
      <Button x:Name="btnMinimize" Width="32" Height="32" Grid.Column="4" BorderThickness="1" BorderBrush="White" VerticalAlignment="Top" Click="BtnMinimize_Click" Background="Transparent">
        <Image Source="include\minimize.png" Width="24" Height="24"/>
      </Button>
      <!-- App Maximize button -->
      <Button x:Name="btnMaximize" Width="32" Height="32" Grid.Column="5" BorderThickness="1" BorderBrush="White" VerticalAlignment="Top" Click="BtnMaximize_Click" Background="Transparent">
        <Image x:Name="btnMaxImage" Width="24" Height="24">
          <Image.Style>
            <Style TargetType="Image">
              <Style.Triggers>
                <DataTrigger Binding="{Binding RelativeSource={RelativeSource Mode=FindAncestor, AncestorType={x:Type Window}}, Path=WindowState}" Value="Normal">
                  <Setter Property="Source" Value="include\maximize.png"/>
                </DataTrigger>
                <DataTrigger Binding="{Binding RelativeSource={RelativeSource Mode=FindAncestor, AncestorType={x:Type Window}}, Path=WindowState}" Value="Maximized">
                  <Setter Property="Source" Value="include\normalize.png"/>
                </DataTrigger>
              </Style.Triggers>
            </Style>
          </Image.Style>
        </Image>
      </Button>
      <!-- App Close button -->
      <Button x:Name="btnClose" Width="32" Height="32" Grid.Column="6" BorderThickness="1" BorderBrush="White" VerticalAlignment="Top" Click="BtnClose_Click" Background="Transparent">
        <Image Source="include\x-neon.png" Width="24" Height="24"/>
      </Button>
    </Grid>
    <Grid Margin="5" DockPanel.Dock="Top">
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto"/>
      <RowDefinition Height="Auto"/>
      <RowDefinition Height="Auto"/>
    </Grid.RowDefinitions>
    <Grid.ColumnDefinitions>
      <ColumnDefinition Width="Auto"/>
      <ColumnDefinition/>
      <ColumnDefinition Width="Auto"/>
    </Grid.ColumnDefinitions>
    <Label Content="Source" Grid.Column="0" Grid.Row="0" HorizontalAlignment="Right" VerticalAlignment="Center"/>
    <TextBox Grid.Column="1" Grid.Row="0" Text="..."/>
    <Button Grid.Column="2" Grid.Row="0" Content="..." Width="26" VerticalAlignment="Stretch"/>
    <Label Content="Destination" Grid.Column="0" Grid.Row="1" HorizontalAlignment="Right" VerticalAlignment="Center"/>
    <TextBox Grid.Column="1" Grid.Row="1" Text="..."/>
    <Button Grid.Column="2" Grid.Row="1" Content="..." Width="26" VerticalAlignment="Stretch"/>
    <Label Content="Log" Grid.Column="0" Grid.Row="2" HorizontalAlignment="Right" VerticalAlignment="Center"/>
    <TextBox Grid.Column="1" Grid.Row="2" Text="..."/>
    <Button Grid.Column="2" Grid.Row="2" Content="..." Width="26" VerticalAlignment="Stretch"/>
  </Grid>
    
  </DockPanel>
</Window>
